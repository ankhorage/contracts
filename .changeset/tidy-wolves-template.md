---
'@ankhorage/contracts': major
---

Add runtime-neutral workload value templates for values composed from literals, secrets, bootstrap credentials and resource outputs. Add a canonical public HTTP(S) base URL to environment networking so services can configure external URLs before startup without runtime-specific interpolation. Persist portable compute targets and safe outputs in the canonical Infra ledger so later CLI processes can resume runtime lifecycle and render outputs without resolved credential values.
