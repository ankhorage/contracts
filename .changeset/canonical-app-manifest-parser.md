---
'@ankhorage/contracts': minor
---

Add canonical runtime parsing and validation for `AppManifest` so consumers can share the Contracts-owned manifest shape instead of maintaining structural guards independently.
