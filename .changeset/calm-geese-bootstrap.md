---
'@ankhorage/contracts': major
---

Allow runtime-neutral workloads to reference keyed control-plane bootstrap credentials, let Cerbos selections carry explicit portable policy files, and require desired runtime targets for status, suspend and destroy so adapters remain stateless across CLI invocations.
